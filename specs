Specs:

An input form captures a string input from the user.
(if input string then the varible that contains string is string)

An output form returns a string to the user.
(this would require a method that captures the input and then returns a string.)

The output page does not show the phrase inputted by the user without obscuring it.

The inputted string is changed to replace all vowels with dashes.


the switchletters method needs to be called from the main class and then then the switchletters class needs to return the value to the createoutput method.


<a class="btn btn-primary" role="button" data-toggle="collapse" href="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
  Link with href
</a>
<button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
  Button with data-target
</button>
<div class="collapse" id="collapseExample">
  <div class="well">
    ...
  </div>
</div>

additional functionality would be to show a reveal button if you got it right. also what happens when you enter two strungs/?
